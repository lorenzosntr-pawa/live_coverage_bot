---
phase: 6.1-country-alerts
plan: 01
type: execute
---

<objective>
Add country name to tournament display in Slack alerts for better context.

Purpose: Improve alert readability by showing country alongside competition name (e.g., "Turkey - 1. Lig" instead of just "1. Lig").
Output: Updated LiveEvent model, both API clients extracting country, Slack messages showing country prefix.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/6.1-country-alerts/6.1-RESEARCH.md

# Prior phase context
@.planning/phases/05-slack-alerts/05-01-SUMMARY.md
@.planning/phases/06-monitoring-loop/06-01-SUMMARY.md

# Source files to modify
@src/live_coverage_bot/clients/models.py
@src/live_coverage_bot/clients/sportybet.py
@src/live_coverage_bot/clients/betpawa.py
@src/live_coverage_bot/clients/slack.py

**Tech stack available:** httpx, pydantic, async patterns
**Established patterns:** LiveEvent model with optional fields, API client parsing methods

**From research (6.1-RESEARCH.md):**
- SportyBet: Country is in `categoryName` field at tournament level
- BetPawa: Country is in `region.name` field at event level
- Both APIs consistently provide country data
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add country_name to LiveEvent and SportyBet extraction</name>
  <files>src/live_coverage_bot/clients/models.py, src/live_coverage_bot/clients/sportybet.py</files>
  <action>
1. In models.py, add `country_name: str | None = None` field to LiveEvent class (after competition_name).

2. In sportybet.py:
   - In `_parse_events()`: Extract `categoryName` from tournament object
   - Update `_parse_single_event()` signature to accept `country_name: str` parameter
   - Pass `country_name` to LiveEvent constructor
  </action>
  <verify>python -c "from live_coverage_bot.clients.models import LiveEvent; e = LiveEvent(event_id='test', home_team='A', away_team='B', competition_id='1', competition_name='League', country_name='Turkey', start_time='2026-01-01T00:00:00Z'); print(e.country_name)"</verify>
  <done>LiveEvent has country_name field, SportyBet client passes categoryName to events</done>
</task>

<task type="auto">
  <name>Task 2: BetPawa country extraction and Slack message update</name>
  <files>src/live_coverage_bot/clients/betpawa.py, src/live_coverage_bot/clients/slack.py</files>
  <action>
1. In betpawa.py `_parse_single_event()`:
   - Extract region from event_data: `region = event_data.get("region", {})`
   - Get country name: `country_name = region.get("name") or None`
   - Pass `country_name=country_name` to LiveEvent constructor

2. In slack.py `_format_message()`:
   - Build competition line with optional country prefix:
     ```python
     if event.country_name:
         competition_line = f"{event.country_name} - {event.competition_name}"
     else:
         competition_line = event.competition_name
     ```
   - Update return statement to use `competition_line` instead of `event.competition_name`
  </action>
  <verify>python -c "from live_coverage_bot.clients.slack import SlackNotifier; from live_coverage_bot.clients.models import LiveEvent; from datetime import datetime, UTC; e = LiveEvent(event_id='test', home_team='Team A', away_team='Team B', competition_id='1', competition_name='1. Lig', country_name='Turkey', start_time=datetime.now(UTC), home_score=1, away_score=0); n = SlackNotifier.__new__(SlackNotifier); print(n._format_message(e))"</verify>
  <done>BetPawa extracts region.name, Slack messages show "Country - Competition" format when country available</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -c "from live_coverage_bot.clients import SportyBetClient, BetPawaClient"` succeeds (imports work)
- [ ] LiveEvent model accepts country_name field
- [ ] Slack message format includes country prefix when present
- [ ] No type errors in modified files
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- country_name field added to LiveEvent model
- SportyBet extracts categoryName from tournament
- BetPawa extracts region.name from event
- Slack alerts show "Country - Competition | Score: X-X" format
</success_criteria>

<output>
After completion, create `.planning/phases/6.1-country-alerts/6.1-01-SUMMARY.md`
</output>
