---
phase: 6.2-srl-filter-provider-info
plan: 01
type: execute
---

<objective>
Filter out SRL test matches from alerts and add provider type/ID to Slack messages.

Purpose: Reduce noise from SportyBet test matches and provide debugging context with provider information.
Output: Cleaner alerts showing only real matches, with provider type and ID visible.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/6.1-country-alerts/6.1-01-SUMMARY.md

# Key files:
@src/live_coverage_bot/clients/models.py
@src/live_coverage_bot/clients/sportybet.py
@src/live_coverage_bot/clients/slack.py

**Tech stack available:** httpx, pydantic, async Python
**Established patterns:** Optional fields, computed_field for provider_ids, mrkdwn Slack formatting

**Constraining decisions:**
- Phase 6.1: country_name is optional field pattern
- Phase 4: provider_ids computed from event_id (SportyBet) or _provider_ids_override (BetPawa)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter SRL test matches in SportyBet client</name>
  <files>src/live_coverage_bot/clients/sportybet.py</files>
  <action>
In `_parse_single_event` method, after extracting `home_team` and `away_team` (around line 150-151), add a check:
- If BOTH `home_team.upper()` contains "SRL" AND `away_team.upper()` contains "SRL", return None early
- Log at DEBUG level when filtering: "Skipping SRL test match: {home_team} vs {away_team}"
- This filters SportyBet's simulated test matches which use "SRL" prefix in team names

Do NOT filter if only ONE team contains "SRL" - only filter when BOTH teams have it.
  </action>
  <verify>grep -n "SRL" src/live_coverage_bot/clients/sportybet.py shows the filter logic</verify>
  <done>SRL check present in _parse_single_event, returns None when both teams contain "SRL"</done>
</task>

<task type="auto">
  <name>Task 2: Add provider info to Slack message format</name>
  <files>src/live_coverage_bot/clients/slack.py</files>
  <action>
In `_format_message` method, add provider information to the message:
- After building the competition_line, add a provider line
- Access `event.provider_ids` (list of ProviderID with type and id fields)
- If provider_ids is not empty, format the first one as: "Provider: {type} #{id}"
- Add this line to the final message after the competition/score line

Final message format should be:
```
🔴 *Missing on BetPawa*
*Home vs Away* (minute')
Country - Competition | Score: X-X
Provider: SPORTRADAR #12345
```

Import ProviderType is NOT needed - just use `provider_id.type` and `provider_id.id` directly.
  </action>
  <verify>grep -n "provider" src/live_coverage_bot/clients/slack.py shows provider formatting logic</verify>
  <done>Provider type and ID appear in Slack message format when provider_ids available</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -c "from live_coverage_bot.clients import SportyBetClient, SlackNotifier"` succeeds
- [ ] grep confirms SRL filter in sportybet.py
- [ ] grep confirms provider formatting in slack.py
</verification>

<success_criteria>

- All tasks completed
- SRL test matches filtered at source (SportyBet client)
- Provider info displayed in Slack alerts
- No import errors or type issues
</success_criteria>

<output>
After completion, create `.planning/phases/6.2-srl-filter-provider-info/6.2-01-SUMMARY.md`
</output>
