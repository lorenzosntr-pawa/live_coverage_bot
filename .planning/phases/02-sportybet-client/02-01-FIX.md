---
phase: 02-sportybet-client
plan: 01-FIX
type: fix
---

<objective>
Fix 1 UAT issue from plan 02-01.

Source: 02-01-ISSUES.md
Priority: 1 blocker
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Issues being fixed:**
@.planning/phases/02-sportybet-client/02-01-ISSUES.md

**Original plan for reference:**
@.planning/phases/02-sportybet-client/02-01-PLAN.md

**Current implementation:**
@src/live_coverage_bot/clients/sportybet.py
@src/live_coverage_bot/config/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research correct SportyBet API endpoint</name>
  <files>N/A (research only)</files>
  <action>
Investigate the correct SportyBet API endpoint for live football events:

1. Check betpawa_api_endpoints/ directory for any reference SportyBet API documentation
2. Test alternative endpoint patterns that SportyBet might use:
   - Different path structures (e.g., `/api/v1/`, `/api/v2/`)
   - Different regional endpoints (remove `/ng/` or try other regions)
   - Check if endpoint requires authentication headers

3. If reference API files exist in project, extract correct endpoint format

Document findings for use in Task 2.

Note: The current endpoint `https://www.sportybet.com/api/ng/liveOrPrematchEvents` returns 404.
  </action>
  <verify>Document correct API endpoint URL and any required parameters/headers</verify>
  <done>Correct SportyBet API endpoint identified with working URL format</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm SportyBet API endpoint to use</decision>
  <context>Research may reveal multiple potential endpoints or require user to provide the correct one based on their environment/region.</context>
  <options>
    <option id="found">
      <name>Endpoint Found</name>
      <pros>Research identified working endpoint</pros>
      <cons>None</cons>
    </option>
    <option id="user-provided">
      <name>User Provides Endpoint</name>
      <pros>User knows correct endpoint for their setup</pros>
      <cons>Depends on user knowledge</cons>
    </option>
    <option id="skip">
      <name>Skip SportyBet for Now</name>
      <pros>Can proceed with BetPawa client first</pros>
      <cons>Delays full functionality</cons>
    </option>
  </options>
  <resume-signal>Select option and provide endpoint URL if applicable</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fix UAT-001 - Update SportyBet API endpoint</name>
  <files>src/live_coverage_bot/config/models.py, src/live_coverage_bot/clients/sportybet.py</files>
  <action>
Update the SportyBet client with the correct API endpoint:

1. Update `SportyBetConfig.base_url` in config/models.py to correct endpoint
2. If needed, update the `/liveOrPrematchEvents` path in sportybet.py
3. If API requires additional headers, add them to httpx.AsyncClient
4. If response structure changed, update `_parse_events()` accordingly

Test with `python test_sportybet.py` (recreate if needed).
  </action>
  <verify>python -c test script fetches live events or returns empty list without 404 error</verify>
  <done>UAT-001 fixed - SportyBet API returns events or empty list, no 404</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] SportyBet API endpoint returns 200 (not 404)
- [ ] Client fetches live events or returns empty list
- [ ] Error handling still works for other error types
- [ ] Original acceptance criteria from 02-01-PLAN.md still met
</verification>

<success_criteria>
- UAT-001 from 02-01-ISSUES.md addressed
- API returns valid response
- Ready for re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-sportybet-client/02-01-FIX-SUMMARY.md`
</output>
