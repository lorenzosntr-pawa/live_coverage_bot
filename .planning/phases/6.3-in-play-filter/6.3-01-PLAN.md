---
phase: 6.3-in-play-filter
plan: 01
type: execute
---

<objective>
Only send alerts for matches that have actually started (minute is set).

Purpose: Reduce false positives from timing differences between platforms — BetPawa may switch to live later than SportyBet.
Output: Filtered alerts that only fire for genuinely in-play matches.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/6.2-srl-filter-provider-info/6.2-01-SUMMARY.md

# Key files:
@src/live_coverage_bot/core/loop.py
@src/live_coverage_bot/clients/models.py

**Constraining decisions:**
- Phase 6.2: Early return pattern for filtering
- Phase 06: Log and continue on errors

**Implementation from roadmap:**
- Add filter in monitoring loop: skip alerts where `event.minute` is None
- Matches showing as "live" but not yet started won't trigger alerts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add in-play filter to monitoring loop</name>
  <files>src/live_coverage_bot/core/loop.py</files>
  <action>
In _poll_cycle method, add a check after the has_been_alerted check (line 111):

```python
# Skip events not yet in-play (no minute set)
if event.minute is None:
    logger.debug("Skipping pre-match event: %s", event.event_id)
    continue
```

This follows the established pattern of early continue for filtered events.
Place AFTER has_been_alerted check to avoid duplicate logging for already-alerted events.
  </action>
  <verify>
1. `python -m py_compile src/live_coverage_bot/core/loop.py` - syntax OK
2. Code review: filter placed after dedup check, before alert send
  </verify>
  <done>
- Events with minute=None are skipped with debug log
- Events with minute set proceed to alert
- Existing flow unchanged for in-play events
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m py_compile src/live_coverage_bot/core/loop.py` succeeds
- [ ] Filter is after has_been_alerted check
- [ ] Filter is before send_missing_event_alert call
- [ ] Debug log includes event_id for traceability
</verification>

<success_criteria>

- All tasks completed
- Filter correctly placed in monitoring loop
- Pre-match events (minute=None) are skipped
- In-play events (minute set) proceed to alert
</success_criteria>

<output>
After completion, create `.planning/phases/6.3-in-play-filter/6.3-01-SUMMARY.md`
</output>
