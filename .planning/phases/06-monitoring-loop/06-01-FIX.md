---
phase: 06-monitoring-loop
plan: 01-FIX
type: fix
---

<objective>
Fix 1 UAT issue from plan 06-01.

Source: 06-01-ISSUES.md
Priority: 0 critical, 1 major, 0 minor
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Issues being fixed:**
@.planning/phases/06-monitoring-loop/06-01-ISSUES.md

**Original plan for reference:**
@.planning/phases/06-monitoring-loop/06-01-PLAN.md

**Key files:**
@src/live_coverage_bot/__main__.py
@src/live_coverage_bot/config/loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UAT-001 - Use load_config() in __main__.py</name>
  <files>src/live_coverage_bot/__main__.py</files>
  <action>
Change the entry point to use `load_config()` instead of `Settings()`:

**Current (broken):**
```python
from live_coverage_bot.config import Settings
# ...
settings = Settings()
```

**Fix:**
```python
from live_coverage_bot.config import load_config
# ...
settings = load_config()
```

This enables the bot to read from config.yaml while still allowing environment variable overrides (as documented in load_config()).
  </action>
  <verify>
Create config.yaml with webhook_url, run `python -m live_coverage_bot` without env vars, verify it starts successfully.
  </verify>
  <done>Bot loads config from config.yaml, UAT-001 resolved</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] UAT-001 fixed (bot reads config.yaml)
- [ ] Environment variable overrides still work (LCB_SLACK__WEBHOOK_URL takes precedence)
- [ ] `python -m ruff check src/live_coverage_bot/__main__.py` passes
</verification>

<success_criteria>
- UAT-001 from 06-01-ISSUES.md addressed
- Bot starts with config.yaml
- Ready for re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/06-monitoring-loop/06-01-FIX-SUMMARY.md`
</output>
