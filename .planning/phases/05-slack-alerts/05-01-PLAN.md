---
phase: 05-slack-alerts
plan: 01
type: execute
---

<objective>
Create Slack notification client for sending missing event alerts via webhook.

Purpose: Enable real-time alerts when SportyBet events are missing from BetPawa coverage.
Output: SlackNotifier class that formats and sends match alerts to Slack webhook.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-matching/04-01-SUMMARY.md

# Key files for reference
@src/live_coverage_bot/clients/sportybet.py
@src/live_coverage_bot/clients/models.py
@src/live_coverage_bot/config/models.py

# Available patterns:
- httpx.AsyncClient with context manager pattern (sportybet.py)
- SlackConfig with webhook_url already defined in config/models.py
- LiveEvent model has: home_team, away_team, competition_name, minute, home_score, away_score, start_time

# Message format requirement (from PROJECT.md):
"Slack webhook alerts with match info (teams, league, score, time) and SportyBet link"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SlackNotifier class</name>
  <files>src/live_coverage_bot/clients/slack.py</files>
  <action>
Create SlackNotifier class following established client patterns:

1. Constructor takes SlackConfig with webhook_url
2. Uses httpx.AsyncClient for HTTP POST requests
3. Implements async context manager (__aenter__/__aexit__)
4. Create send_missing_event_alert(event: LiveEvent) method:
   - Format message with mrkdwn:
     ```
     🔴 *Missing on BetPawa*
     *{home_team} vs {away_team}* ({minute}')
     {competition_name} | Score: {home_score}-{away_score}
     ```
   - If minute is None, omit the minute display
   - If scores are None, show "Score: -"
   - POST to webhook_url with JSON {"text": message}
   - Return True on success (2xx), False otherwise
5. Create SlackError exception class for error handling
6. Add logging similar to other clients

Do NOT include SportyBet link yet - we don't have a link construction function. That can be added in the monitoring loop phase when we know the URL pattern.
  </action>
  <verify>python -m ruff check src/live_coverage_bot/clients/slack.py</verify>
  <done>SlackNotifier class created with send_missing_event_alert method, follows async client pattern</done>
</task>

<task type="auto">
  <name>Task 2: Export SlackNotifier from clients package</name>
  <files>src/live_coverage_bot/clients/__init__.py</files>
  <action>
Add SlackNotifier and SlackError to the clients package exports:
1. Import SlackNotifier and SlackError from slack module
2. Add both to __all__ list

Follow existing export pattern (SportyBetClient, BetPawaClient, etc. should already be exported).
  </action>
  <verify>python -c "from live_coverage_bot.clients import SlackNotifier, SlackError"</verify>
  <done>SlackNotifier and SlackError importable from live_coverage_bot.clients</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m ruff check src/live_coverage_bot/clients/` passes
- [ ] `python -c "from live_coverage_bot.clients import SlackNotifier, SlackError"` works
- [ ] SlackNotifier follows async context manager pattern
- [ ] Message format includes teams, competition, minute, score
</verification>

<success_criteria>

- SlackNotifier class created with async webhook support
- send_missing_event_alert method formats LiveEvent for Slack
- Error handling with SlackError exception
- Package exports updated
- All verification checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-slack-alerts/05-01-SUMMARY.md` following summary template.
</output>
