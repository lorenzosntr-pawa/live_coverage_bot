---
phase: 6.5-human-readable-logging
plan: 01
type: execute
---

<objective>
Replace verbose httpx logs with human-readable tournament/event summaries in poll cycle output.

Purpose: Make monitoring logs scannable and useful for operators - see what's happening at a glance.
Output: Readable logs showing events grouped by tournament with teams, scores, and minutes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@src/live_coverage_bot/__main__.py
@src/live_coverage_bot/core/loop.py
@src/live_coverage_bot/clients/betpawa.py
@src/live_coverage_bot/core/prematch_cache.py
@src/live_coverage_bot/clients/models.py

**Prior decisions:**
- Phase 6.4: get_upcoming_events() returns dict[str, datetime] with provider ID keys
- Pre-match cache stores provider IDs for filtering alerts
- LiveEvent model has full event data (teams, tournament, scores, minute)

**Requirements from roadmap:**
- Suppress verbose httpx request logging (set to WARNING)
- Log grouped summary by tournament: "England - Premier League: 3 events"
- Log individual events: "Man Utd vs Liverpool | 2-1 | 45'"
- Separate sections for SportyBet, BetPawa, Missing events
- Keep summary line: "Poll: 11 SportyBet, 8 BetPawa, 3 missing, 0 alerts"
- Pre-match cache refresh: log readable event list with teams, tournament, kickoff time
- IMPORTANT: Preserve both SPORTRADAR and GENIUSSPORTS IDs per event (same match can have different providers between platforms)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UpcomingEvent model and enhance get_upcoming_events()</name>
  <files>src/live_coverage_bot/clients/models.py, src/live_coverage_bot/clients/betpawa.py</files>
  <action>
  1. Add UpcomingEvent model to models.py:
     - home_team: str
     - away_team: str
     - competition_name: str
     - country_name: str | None
     - start_time: datetime
     - provider_ids: list[ProviderID] (preserves BOTH SPORTRADAR and GENIUSSPORTS)

  2. Modify get_upcoming_events() in betpawa.py:
     - Return list[UpcomingEvent] instead of dict[str, datetime]
     - Parse full event data (teams, competition, region, start_time)
     - Extract ALL provider IDs from widgets (not just first)
     - Remove time filtering (let PreMatchCache handle expiry)

  3. Update imports in betpawa.py to include UpcomingEvent
  </action>
  <verify>python -c "from live_coverage_bot.clients.models import UpcomingEvent; from live_coverage_bot.clients.betpawa import BetPawaClient; print('OK')"</verify>
  <done>UpcomingEvent model exists, get_upcoming_events() returns list[UpcomingEvent] with full event data and all provider IDs</done>
</task>

<task type="auto">
  <name>Task 2: Update PreMatchCache and create logging formatter</name>
  <files>src/live_coverage_bot/core/prematch_cache.py, src/live_coverage_bot/core/formatter.py, src/live_coverage_bot/core/__init__.py</files>
  <action>
  1. Update PreMatchCache.update() to accept list[UpcomingEvent]:
     - Store events by provider ID keys for was_offered_prematch() lookup
     - Keep list of events for logging access
     - Add events property to retrieve cached events

  2. Create formatter.py with functions:
     - format_events_by_tournament(events: list[LiveEvent | UpcomingEvent]) -> str
       Groups events by "country - competition" (or just competition if no country)
       Each group shows count header, then individual events
       Format per event: "Home vs Away | X-X | MM'" (live) or "Home vs Away @ HH:MM" (upcoming)
     - format_poll_summary(sportybet: list, betpawa: list, missing: list, alerted: int) -> str
       Outputs sections: "=== SportyBet (N) ===" then formatted events, same for BetPawa/Missing
       Ends with summary line

  3. Export formatter from core/__init__.py
  </action>
  <verify>python -c "from live_coverage_bot.core import PreMatchCache; from live_coverage_bot.core.formatter import format_events_by_tournament; print('OK')"</verify>
  <done>PreMatchCache accepts full events, formatter produces grouped readable output</done>
</task>

<task type="auto">
  <name>Task 3: Suppress httpx logs and integrate formatter into MonitoringLoop</name>
  <files>src/live_coverage_bot/__main__.py, src/live_coverage_bot/core/loop.py</files>
  <action>
  1. In __main__.py after basicConfig():
     - Set httpx logger to WARNING: logging.getLogger("httpx").setLevel(logging.WARNING)
     - Also suppress httpcore: logging.getLogger("httpcore").setLevel(logging.WARNING)

  2. In loop.py _poll_cycle():
     - Import formatter functions
     - Replace current logger.info with formatted output:
       - Log full event lists only at DEBUG level (for troubleshooting)
       - At INFO level: use format_poll_summary() for readable output
     - For pre-match cache refresh:
       - Use format_events_by_tournament() to log cached upcoming events
       - Log "Pre-match cache refreshed: N events" followed by formatted list

  3. Remove redundant debug logging that duplicates formatted output
  </action>
  <verify>python -c "from live_coverage_bot import __main__; from live_coverage_bot.core.loop import MonitoringLoop; print('OK')"</verify>
  <done>httpx logs suppressed, poll cycle shows readable grouped events, pre-match refresh shows formatted upcoming events</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m py_compile src/live_coverage_bot/**/*.py` - no syntax errors
- [ ] `python -c "from live_coverage_bot.core import MonitoringLoop"` - imports work
- [ ] Run bot briefly and verify:
  - [ ] No httpx request logging spam
  - [ ] Poll cycles show events grouped by tournament
  - [ ] Pre-match cache refresh shows readable event list
  - [ ] Summary line still present: "Poll: X SportyBet, Y BetPawa, Z missing, W alerts"
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No import errors or syntax errors
- Logs are human-readable and scannable
- Provider IDs preserved for matching (both SPORTRADAR and GENIUSSPORTS)
</success_criteria>

<output>
After completion, create `.planning/phases/6.5-human-readable-logging/6.5-01-SUMMARY.md`
</output>
