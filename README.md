# Live Coverage Bot

Monitor sports events across betting platforms and notify via Slack when live coverage becomes available.

## Installation

```bash
pip install -e ".[dev]"
```

## Configuration

Copy `config.example.yaml` to `config.yaml` and fill in your settings.

## Usage

```bash
python -m live_coverage_bot
```
